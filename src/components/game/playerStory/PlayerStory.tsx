import * as React from 'react'

type PlayerStoryProps = {
}

type PlayerStoryState = {
}

export default class PlayerStory extends React.Component<PlayerStoryProps, PlayerStoryState> {
  render() {
    return (
      <div>player story</div>
    )
  }
}
